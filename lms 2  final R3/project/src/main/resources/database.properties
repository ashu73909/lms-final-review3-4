```properties
# Database Connection
jdbcUrl=jdbc:mysql://localhost:3306/edulearn
username=root
password=your_password

# Connection Pool Settings
maximumPoolSize=10
minimumIdle=5
connectionTimeout=30000
idleTimeout=600000
maxLifetime=1800000

# Performance Tuning
cachePrepStmts=true
prepStmtCacheSize=250
prepStmtCacheSqlLimit=2048
useServerPrepStmts=true

# Reliability Settings
autoReconnect=true
useUnicode=true
characterEncoding=UTF-8
serverTimezone=UTC

# Logging
logLevel=INFO

```

Added:
- Connection timeouts
- Prepared statement caching
- Character encoding
- Auto-reconnect
- Server timezone
- Performance optimizations

Save this as `database.properties` in `src/main/resources/`.